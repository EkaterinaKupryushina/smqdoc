@model MvcFront.Models.FieldTemplateEditModel

@{
    ViewBag.Title = "AddField";
}

<h2>Добавить поле</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    
    <fieldset>
    @Html.EditorForModel()
       
    </fieldset>
     <p>
            <input type="submit" value="Создать" />|
            @Html.ActionLink("Отмена", "DocTemplateFieldsManagment", new { id = Model.DocTemplateId })
        </p>
}
