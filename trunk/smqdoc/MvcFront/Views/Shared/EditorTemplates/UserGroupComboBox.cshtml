@using MvcFront.DB
@using Telerik.Web.Mvc.UI
@*@model MvcFront.Models.UserGroupListViewModel*@

@{
       @(Html.Telerik().ComboBox()
                             .Name(ViewData.TemplateInfo.GetFullHtmlFieldName("") ?? "UserGroup_usergroupid")
            .SelectedIndex(0)
            .DataBinding(binding => binding.Ajax().Select("AjaxUserGroupList", "Dictionary").Cache(true)            
            ))
}

